version: "2"

linters:
  default: standard
  enable:
    - misspell
    - revive
  settings:
    revive:
      rules:
        - name: var-naming
          disabled: true   # we deliberately use SchedEcsParameters etc.
